.col-xs-6.col-sm-4
  %h1 Edit cat: #{@cat.name}
  - unless @cat.image.empty?
    %p.cat-show
      = image_tag @cat.image
  = render 'form'

  = link_to "Show", @cat, class: 'btn btn-primary btn-xs'
  \|
  = link_to 'Back', cats_path, class: 'btn btn-primary btn-xs'
